-- ============================================================
-- 一次性数据迁移：把「计算机2101班」专属排课改为「全班级」
--
-- 背景：线上 16 行排课的 class_name 全填「计算机2101班」，但平台实际有 8 个班，
-- 且这 8 门课实际面向所有选课学生。显式班级导致 4 名学生（何洋/罗彤/杨洋/王明，
-- 共 5 人次）无法进入 AI 分层测试。班级与学员关系改由授课教师在课程内维护。
--
-- ⚠️ 执行前务必先跑「第 1 步：备份」。执行后计算机2101班的 12 名学生课程列表会清空
--    （他们没有选课记录兜底），需教师随后在课程内重新导入学员名单。
--
-- 生成时间：2026-09-16
-- ============================================================

-- ------------------------------------------------------------
-- 第 1 步：备份（务必先执行，并把结果保存下来）
-- ------------------------------------------------------------
-- 1a) 完整备份这 16 行的所有字段，便于逐行还原
SELECT id, course_id, title, teacher, mentor, semester, room,
       class_name, day, start_date, end_date, time_slot
FROM schedules
WHERE id BETWEEN 12 AND 27
ORDER BY id;

-- 1b) 整表备份（更强保险，建议一并保存）
-- SELECT * FROM schedules ORDER BY id;


-- ------------------------------------------------------------
-- 第 2 步：核对影响面（应为 16 行，且 class_name 全为「计算机2101班」）
-- ------------------------------------------------------------
SELECT COUNT(*) AS affected_rows
FROM schedules
WHERE TRIM(COALESCE(class_name, '')) <> ''
  AND id BETWEEN 12 AND 27;


-- ------------------------------------------------------------
-- 第 3 步：执行迁移
-- 空 = 全班级（对该课程所有已选课学生生效），与读侧 TRIM(COALESCE(...)) 口径一致
-- ------------------------------------------------------------
UPDATE schedules
SET class_name = NULL
WHERE id BETWEEN 12 AND 27;


-- ------------------------------------------------------------
-- 第 4 步：验证（应返回 16 行，class_name 全为 NULL）
-- ------------------------------------------------------------
SELECT id, class_name FROM schedules WHERE id BETWEEN 12 AND 27 ORDER BY id;


-- ------------------------------------------------------------
-- 回滚（如需撤销）
-- ------------------------------------------------------------
-- UPDATE schedules SET class_name = '计算机2101班' WHERE id BETWEEN 12 AND 27;
